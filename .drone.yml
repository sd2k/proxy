clone:
  git: 
     image: plugins/git
pipeline:
  build:
    image: gcr.io/istio-testing/istio-builder:latest
    commands:
      - make build_envoy
  test:
    image: gcr.io/istio-testing/istio-builder:latest
    commands:
      - make test_envoy


